import { GET_POLICECALLS } from './actionTypes';

export const getPoliceCalls = () => {
    // Dispatch to store and make aware that data is being fetched
    // fetch data
    // when data reutrns dispatch to store you have received data and send it as payload
}